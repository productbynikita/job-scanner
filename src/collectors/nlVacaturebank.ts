import { buildCountryLeaderCollector } from './countryLeader.js';

export const nlVacaturebankCollector = buildCountryLeaderCollector({
  collectorId: 'nl_vacaturebank',
  sourceKey: 'nl_vacaturebank',
  name: 'NationaleVacaturebank',
});
